@using BulkyBook.Presentation.ViewModels
@model IEnumerable<CoverTypeViewModel>
@{
    ViewData["Title"] = "Cover Types";
}

<div class="d-flex flex-row align-items-center w-100 my-2">
    <h1 class="me-auto text-primary">
        Cover Types
    </h1>
    <div>
        <a asp-controller="CoverTypes" asp-action="Create" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i>
            <span class="ms-1">New Cover Type</span>
        </a>
    </div>
</div>

<table class="table table-striped table-bordered w-100">
    <thead class="table-dark">
    <tr>
        <th class="pb-4">
            Cover Type Name
        </th>
        <th width="180"></th>
    </tr>
    </thead>
    <tbody>
    @foreach(var coverType in Model)
    {
        <tr>
            <td>@coverType.Name</td>
            <td class="d-flex flex-row justify-content-center gap-2">
                <div class='d-grid flex-fill'>
                    <a asp-controller="CoverTypes" asp-action="Edit" asp-route-id="@coverType.Id" class="btn btn-info btn-sm">
                        <i class="bi bi-pencil-square"></i> <span class="ms-1"> Edit</span>
                    </a>
                </div>
                <div class="d-grid flex-fill">
                    <a asp-controller="CoverTypes" asp-action="Delete" asp-route-id="@coverType.Id" class="btn btn-danger btn-sm">
                        <i class="bi bi-trash"></i><span class="ms-1">Delete</span>
                    </a>
                </div>
            </td>
        </tr>
    }
    </tbody>
</table>

